---
all:
  hosts:
    lhe-dev:
      ansible_ssh_host: 192.168.178.172
      ansible_user: ubuntu
  vars:
    upgrade_os: no
    docker_repo_distro_release: "hirsute"
    target_users:
      luca:
        groups: "admin,docker"
        ssh_key: https://github.com/luca-heitmann.keys
        allow_passwordless_sudo: yes
        ohmyzsh_install: yes
        ohmyzsh_force_replace_zshrc: no
